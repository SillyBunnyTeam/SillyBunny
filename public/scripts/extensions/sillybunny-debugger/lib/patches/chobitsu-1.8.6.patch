diff --git a/src/domains/DOM.ts b/src/domains/DOM.ts
index 618798e..c35a9da 100644
--- a/src/domains/DOM.ts
+++ b/src/domains/DOM.ts
@@ -68,0 +69,8 @@ export function enable() {
+export function disable() {
+  isEnable = false
+  mutationObserver.disconnect()
+  nodeManager.clear()
+  searchResults.clear()
+  history.length = 0
+}
+
@@ -74 +82 @@ function hookAttachShadow() {
-      if (!nodeManager.isValidNode(this)) {
+      if (!isEnable || !nodeManager.isValidNode(this)) {
@@ -78,10 +86,8 @@ function hookAttachShadow() {
-      ;(this as any).chobitsuShadowRoot = shadowRoot
-      if (isEnable) {
-        mutationObserver.observe(shadowRoot)
-        const hostId = getNodeId(this)
-        if (hostId) {
-          connector.trigger('DOM.shadowRootPushed', {
-            hostId,
-            root: nodeManager.wrap(shadowRoot, { depth: 1 }),
-          })
-        }
+      nodeManager.setShadowRoot(this, shadowRoot)
+      mutationObserver.observe(shadowRoot)
+      const hostId = getNodeId(this)
+      if (hostId) {
+        connector.trigger('DOM.shadowRootPushed', {
+          hostId,
+          root: nodeManager.wrap(shadowRoot, { depth: 1 }),
+        })
diff --git a/src/domains/Network.ts b/src/domains/Network.ts
index cf540b2..786cb70 100644
--- a/src/domains/Network.ts
+++ b/src/domains/Network.ts
@@ -12 +11,0 @@ import isUndef from 'licia/isUndef'
-import types from 'licia/types'
@@ -43,0 +43 @@ const resTxtMap = new Map()
+const MAX_RESPONSE_BODIES = 100
@@ -45,0 +46 @@ let isEnable = false
+let generation = 0
@@ -47,0 +49 @@ export const enable = function () {
+  if (!isEnable) generation += 1
@@ -49,2 +51,6 @@ export const enable = function () {
-  each(triggers, trigger => trigger())
-  triggers = []
+}
+
+export function disable() {
+  if (isEnable) generation += 1
+  isEnable = false
+  resTxtMap.clear()
@@ -63,0 +70 @@ function enableXhr() {
+  const requests = new WeakMap<XMLHttpRequest, XhrRequest>()
@@ -70,2 +77,19 @@ function enableXhr() {
-    if (!isValidUrl(url)) {
-      return origOpen.apply(this, arguments)
+    const result = origOpen.apply(this, arguments)
+    requests.delete(this)
+    if (isEnable && isValidUrl(url)) {
+      const requestGeneration = generation
+      const req = new XhrRequest(
+        this,
+        method,
+        url,
+        () => isEnable && generation === requestGeneration
+      )
+      requests.set(this, req)
+      this.addEventListener(
+        'loadend',
+        () => {
+          if (requests.get(this) === req) requests.delete(this)
+        },
+        { once: true }
+      )
+      bindRequestEvent(req, 'XHR')
@@ -73,12 +97 @@ function enableXhr() {
-
-    const xhr = this
-
-    const req = ((xhr as any).chobitsuRequest = new XhrRequest(
-      xhr,
-      method,
-      url
-    ))
-
-    bindRequestEvent(req, 'XHR')
-
-    origOpen.apply(this, arguments)
+    return result
@@ -88,2 +101,2 @@ function enableXhr() {
-    const req = (this as any).chobitsuRequest
-    if (req) req.handleSend(data)
+    const req = requests.get(this)
+    if (isEnable && req) req.handleSend(data)
@@ -95,2 +108,2 @@ function enableXhr() {
-    const req = (this as any).chobitsuRequest
-    if (req) {
+    const req = requests.get(this)
+    if (isEnable && req) {
@@ -123,2 +135,0 @@ function enableFetch() {
-    const req = new FetchRequest(...args)
-    bindRequestEvent(req, 'Fetch')
@@ -126 +137,14 @@ function enableFetch() {
-    req.send(fetchResult)
+    if (isEnable) {
+      const requestGeneration = generation
+      try {
+        const req = new FetchRequest(
+          args[0],
+          args[1],
+          () => isEnable && generation === requestGeneration
+        )
+        bindRequestEvent(req, 'Fetch')
+        req.send(fetchResult)
+      } catch (e) {
+        // Instrumentation must not break an otherwise valid fetch.
+      }
+    }
@@ -133,0 +158 @@ function bindRequestEvent(req: Emitter, type: string) {
+    if (!isEnable) return
@@ -150,0 +176 @@ function bindRequestEvent(req: Emitter, type: string) {
+    if (!isEnable) return
@@ -157,0 +184 @@ function bindRequestEvent(req: Emitter, type: string) {
+    if (!isEnable) return
@@ -170,0 +198,3 @@ function bindRequestEvent(req: Emitter, type: string) {
+    if (resTxtMap.size >= MAX_RESPONSE_BODIES) {
+      resTxtMap.delete(resTxtMap.keys().next().value)
+    }
@@ -178,0 +209 @@ function bindRequestEvent(req: Emitter, type: string) {
+    if (!isEnable) return
@@ -193 +224 @@ function enableWebSocket() {
-    if (!isValidUrl(url)) {
+    if (!isEnable || !isValidUrl(url)) {
@@ -196,0 +228,3 @@ function enableWebSocket() {
+    const socketGeneration = generation
+    const shouldCapture = () =>
+      isEnable && generation === socketGeneration
@@ -204,0 +239 @@ function enableWebSocket() {
+      if (!shouldCapture()) return
@@ -222,0 +258 @@ function enableWebSocket() {
+      if (!shouldCapture()) return
@@ -233,0 +270 @@ function enableWebSocket() {
+        if (!shouldCapture()) return
@@ -249 +286 @@ function enableWebSocket() {
-      if (!isUndef(data)) {
+      if (shouldCapture() && !isUndef(data)) {
@@ -256,0 +294 @@ function enableWebSocket() {
+      if (!shouldCapture()) return
@@ -263,0 +302 @@ function enableWebSocket() {
+        if (!shouldCapture()) return
@@ -277,0 +317 @@ function enableWebSocket() {
+      if (!shouldCapture()) return
@@ -284,0 +325 @@ function enableWebSocket() {
+      if (!shouldCapture()) return
@@ -306,2 +346,0 @@ function isValidUrl(url: string) {
-let triggers: types.AnyFn[] = []
-
@@ -309,5 +348 @@ function trigger(method: string, params: any) {
-  if (isEnable) {
-    connector.trigger(method, params)
-  } else {
-    triggers.push(() => connector.trigger(method, params))
-  }
+  if (isEnable) connector.trigger(method, params)
diff --git a/src/domains/Overlay.ts b/src/domains/Overlay.ts
index 9be02ad..7bc7270 100644
--- a/src/domains/Overlay.ts
+++ b/src/domains/Overlay.ts
@@ -18 +18 @@ let domHighlighter: LunaDomHighlighter
-let isCssLoaded = false
+let fallbackStyle: HTMLStyleElement | null = null
@@ -63,4 +63 @@ export function enable() {
-    if (!isCssLoaded) {
-      evalCss(css)
-      isCssLoaded = true
-    }
+    fallbackStyle = evalCss(css)
@@ -74,0 +72 @@ export function enable() {
+  addInspectEvents()
@@ -79,0 +78,6 @@ export function disable() {
+  if (!isEnable) return
+
+  inspectMode = 'none'
+  highlightConfig = {}
+  lastNodeId = -1
+  removeInspectEvents()
@@ -81,0 +86,2 @@ export function disable() {
+  fallbackStyle?.remove()
+  fallbackStyle = null
@@ -82,0 +89,4 @@ export function disable() {
+  if (viewportSizeTimer) clearTimeout(viewportSizeTimer)
+  viewportSizeTimer = null
+  $viewportSize.remove()
+  showViewportSizeOnResize = false
@@ -192,8 +202,3 @@ function addEvent(type: string, listener: any) {
-if (hasTouchSupport) {
-  addEvent('touchstart', moveListener)
-  addEvent('touchmove', moveListener)
-  addEvent('touchend', clickListener)
-} else {
-  addEvent('mousemove', moveListener)
-  addEvent('mouseout', outListener)
-  addEvent('click', clickListener)
+
+function removeEvent(type: string, listener: any) {
+  document.documentElement.removeEventListener(type, listener, true)
@@ -201,0 +207,15 @@ if (hasTouchSupport) {
+function inspectEvents(action: typeof addEvent) {
+  if (hasTouchSupport) {
+    action('touchstart', moveListener)
+    action('touchmove', moveListener)
+    action('touchend', clickListener)
+  } else {
+    action('mousemove', moveListener)
+    action('mouseout', outListener)
+    action('click', clickListener)
+  }
+}
+
+const addInspectEvents = () => inspectEvents(addEvent)
+const removeInspectEvents = () => inspectEvents(removeEvent)
+
diff --git a/src/lib/nodeManager.ts b/src/lib/nodeManager.ts
index 526989a..df49fad 100644
--- a/src/lib/nodeManager.ts
+++ b/src/lib/nodeManager.ts
@@ -10,0 +11 @@ const nodeIds = new Map()
+const shadowRoots = new WeakMap()
@@ -12,0 +14,4 @@ let id = 1
+export function setShadowRoot(host: Element, root: ShadowRoot) {
+  shadowRoots.set(host, root)
+}
+
@@ -62,4 +67,3 @@ export function wrap(node: any, { depth = 1 } = {}) {
-  if (node.shadowRoot) {
-    ret.shadowRoots = [wrap(node.shadowRoot, { depth: 1 })]
-  } else if (node.chobitsuShadowRoot) {
-    ret.shadowRoots = [wrap(node.chobitsuShadowRoot, { depth: 1 })]
+  const shadowRoot = node.shadowRoot || shadowRoots.get(node)
+  if (shadowRoot) {
+    ret.shadowRoots = [wrap(shadowRoot, { depth: 1 })]
diff --git a/src/lib/request.ts b/src/lib/request.ts
index 7130da2..bdbb43b 100644
--- a/src/lib/request.ts
+++ b/src/lib/request.ts
@@ -12,0 +13,6 @@ import { createId } from './util'
+const BODY_LIMIT = 256 * 1024
+
+function limitBody(value: string) {
+  return value.length > BODY_LIMIT ? value.slice(0, BODY_LIMIT) : value
+}
+
@@ -19 +25,7 @@ export class XhrRequest extends Emitter {
-  constructor(xhr: XMLHttpRequest, method: string, url: string) {
+  private shouldCapture: () => boolean
+  constructor(
+    xhr: XMLHttpRequest,
+    method: string,
+    url: string,
+    shouldCapture: () => boolean = () => true
+  ) {
@@ -26,0 +39 @@ export class XhrRequest extends Emitter {
+    this.shouldCapture = shouldCapture
@@ -28 +41,5 @@ export class XhrRequest extends Emitter {
-    xhr.addEventListener('readystatechange', () => {
+    const onReadyStateChange = () => {
+      if (xhr.readyState === 4) {
+        xhr.removeEventListener('readystatechange', onReadyStateChange)
+      }
+      if (!this.shouldCapture()) return
@@ -38 +55,2 @@ export class XhrRequest extends Emitter {
-    })
+    }
+    xhr.addEventListener('readystatechange', onReadyStateChange)
@@ -48,0 +67 @@ export class XhrRequest extends Emitter {
+    if (!this.shouldCapture()) return
@@ -49,0 +69 @@ export class XhrRequest extends Emitter {
+    data = limitBody(data)
@@ -64,0 +85 @@ export class XhrRequest extends Emitter {
+    if (!this.shouldCapture()) return
@@ -86,0 +108 @@ export class XhrRequest extends Emitter {
+      if (!this.shouldCapture()) return
@@ -103 +125 @@ export class XhrRequest extends Emitter {
-        if (result) resTxt = result
+        if (result) resTxt = limitBody(result)
@@ -107,2 +129,3 @@ export class XhrRequest extends Emitter {
-      if (resType === '' || resType === 'text') resTxt = xhr.responseText
-      if (resType === 'json') resTxt = JSON.stringify(xhr.response)
+      if (resType === '' || resType === 'text')
+        resTxt = limitBody(xhr.responseText)
+      if (resType === 'json') resTxt = limitBody(JSON.stringify(xhr.response))
@@ -127 +150,6 @@ export class FetchRequest extends Emitter {
-  constructor(input: any, options: any = {}) {
+  private shouldCapture: () => boolean
+  constructor(
+    input: any,
+    options: any = {},
+    shouldCapture: () => boolean = () => true
+  ) {
@@ -137,0 +166 @@ export class FetchRequest extends Emitter {
+    this.shouldCapture = shouldCapture
@@ -139,0 +169 @@ export class FetchRequest extends Emitter {
+    if (!this.shouldCapture()) return
@@ -142 +172 @@ export class FetchRequest extends Emitter {
-    const data = isStr(options.body) ? options.body : ''
+    const data = isStr(options.body) ? limitBody(options.body) : ''
@@ -154,0 +185 @@ export class FetchRequest extends Emitter {
+        if (!this.shouldCapture()) return
@@ -158,0 +190,2 @@ export class FetchRequest extends Emitter {
+          if (!this.shouldCapture()) return
+          resTxt = limitBody(resTxt)
@@ -176,0 +210 @@ export class FetchRequest extends Emitter {
+        if (!this.shouldCapture()) return
